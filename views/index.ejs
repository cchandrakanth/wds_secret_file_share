<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secret file upload</title>
</head>
<body>
    <!-- All the code comes with the following template would be executed on the server side rather than on the client side -->
    <% if (locals.fileLink != null) { %>
        <div style="margin-bottom: 1.2rem;">
            Your file is upload
            <a href="<%= locals.fileLink %>"><%= locals.fileLink %></a>
            <!-- Above template means that whatever the value present would be set to the location of the browser -->
        </div>
    <%} %>
    <!-- enctype="multipart/form-data" means send the file in multiple parts -->
    <form action="/upload" method="post" enctype="multipart/form-data"
    style="display: grid; gap: .5rem; grid-template-columns: auto 1fr; max-width: 500px; margin: 0 auto;">
        <label for="file">File:</label>
        <input type="file" id="file" name="file" required>
        <label for="password">Password:</label>
        <input type="text" id="password" name="password">
        <button type="submit" style="grid-column: span 2; background-color: #8B74BD;">Share</button>
    </form>
</body>
</html>